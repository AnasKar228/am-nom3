# EDA-отчёт

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Параметры отчёта

- `max_hist_columns`: **6**
- `top_k_categories`: **5**
- `min_missing_share` (порог проблемных пропусков): **20%**

## Качество данных (эвристики)

- Оценка качества: **0.49**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Константные колонки: **False**, кол-во: **0**, список: —
- High-cardinality категориальные: **True**, кол-во: **1**, детали: city (44% unique)
- Дубликаты в ID-колонках: **True**, колонки: user_id

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

Колонок с долей пропусков ≥ 20% не обнаружено.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

Для каждой категориальной колонки сохранены top-5 значений (см. файлы в папке `top_categories/`).

## Гистограммы числовых колонок

Построены гистограммы не более чем для **6** числовых колонок. См. файлы `hist_*.png`.
