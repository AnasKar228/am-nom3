# Отчёт по example.csv

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Параметры отчёта

- `max_hist_columns`: **4**
- `top_k_categories`: **3**
- `min_missing_share` (порог проблемных пропусков): **5%**

## Качество данных (эвристики)

- Оценка качества: **0.49**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Константные колонки: **False** ( если есть)
- High-cardinality категориальные: **True** (city если есть)
- Дубликаты в ID-колонках: **True** (user_id если есть)

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

Колонки с долей пропусков ≥ 5%:

- **city**: 5.56% пропусков
- **avg_session_duration_min**: 5.56% пропусков

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

Для кажой категориальной колонки сохранены top-3 значений (см. файлы в папке `top_categories/`).

## Гистограммы числовых колонок

Построены гистограммы не более чем для **4** числовых колонок. См. файлы `hist_*.png`.
